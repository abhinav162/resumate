services:
  backend:
    image: ghcr.io/${GHCR_USERNAME:-abhinav162}/resumate-backend:${IMAGE_TAG:-latest}
    build:
      context: .
      dockerfile: apps/backend/Dockerfile
    ports:
      - "${BACKEND_PORT:-4300}:4300"
    environment:
      - NODE_ENV=production
      - PORT=4300
    env_file:
      - apps/backend/.env # standard .env file, swapped by deploy script
    volumes:
      - ./data:/app/data
    restart: always

  frontend:
    image: ghcr.io/${GHCR_USERNAME:-abhinav162}/resumate-frontend:${IMAGE_TAG:-latest}
    build:
      context: .
      dockerfile: apps/frontend-v2/Dockerfile
    ports:
      - "${FRONTEND_PORT:-3160}:80"
    env_file:
      - apps/frontend-v2/.env # standard .env file, swapped by deploy script
    restart: always
    depends_on:
      - backend
