# Frontend Environment Variables
# Copy this file to .env.production for production deployment

# API URL - with nginx routing, frontend uses /api prefix
VITE_API_URL=http://localhost:3160/api
NODE_ENV=production